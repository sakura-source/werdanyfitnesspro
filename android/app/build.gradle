android {
    namespace = "com.example.werdanyfitnesspro"
    defaultConfig {
        applicationId = "com.example.werdanyfitnesspro"
        minSdkVersion 16
        targetSdkVersion 30
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
